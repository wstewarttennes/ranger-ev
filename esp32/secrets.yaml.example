# Copy to secrets.yaml and fill in. secrets.yaml is gitignored — never commit it.
wifi_ssid: "your-truck-or-home-wifi"
wifi_password: "..."
# Generate: `openssl rand -base64 32`  (or let `esphome run` create one on first flash)
api_key: "base64-32-byte-key-here"
ota_password: "any-password-you-pick"
